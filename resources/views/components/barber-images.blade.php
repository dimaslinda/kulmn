<div class="flex-1 flex flex-row justify-center gap-5 mt-5">
    <div class="max-w-sm">
        <div class="bg-tombol w-full h-5 md:h-10">
        </div>
        <img src="{{ asset($image1) }}" alt="{{ $alt1 }}">
    </div>
    <div class="max-w-sm">
        <img src="{{ asset($image2) }}" alt="{{ $alt2 }}">
        <div class="bg-tombol w-full h-5 md:h-10">
        </div>
    </div>
</div>